@model IEnumerable<MOTILab3.Models.ArmourResistanceResult>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ArmourId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WeaponId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResistedDamage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ArmourId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WeaponId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResistedDamage)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ResultId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ResultId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ResultId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
