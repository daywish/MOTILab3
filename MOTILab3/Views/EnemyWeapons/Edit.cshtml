@model MOTILab3.Models.EnemyWeapon

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EnemyWeapon</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WeaponId" />
            <div class="form-group">
                <label asp-for="WeaponName" class="control-label"></label>
                <input asp-for="WeaponName" class="form-control" />
                <span asp-validation-for="WeaponName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WeaponPhysicalDamage" class="control-label"></label>
                <input asp-for="WeaponPhysicalDamage" class="form-control" />
                <span asp-validation-for="WeaponPhysicalDamage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WeaponMagicalDamage" class="control-label"></label>
                <input asp-for="WeaponMagicalDamage" class="form-control" />
                <span asp-validation-for="WeaponMagicalDamage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
